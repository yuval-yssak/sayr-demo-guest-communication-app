import { Path, Key, ParseOptions, TokensToRegexpOptions } from 'path-to-regexp'
// pathToRegexp.pathToRegexp()
// pathToRegexp
// export default function route(options:any):function

export interface PathRegExp extends RegExp {
  // An array to be populated with the keys found in the path.
  keys: Key[]
}

export default function (
  options?:
    | (pathToRegexp.TokensToRegexpOptions & pathToRegexp.ParseOptions)
    | undefined
): (
  path: Path
) => (pathname: string, params: any) => boolean | { [key: string]: string }
